---
title: PaceFlow 修复提交严格审计 audit-2026-06-05-3625f0d-to-cdb3b03
audit-date: 2026-06-05T12:30:00+08:00
git-range: 3625f0d..cdb3b03
git-head-short: cdb3b03
prior-audit: docs/audits/audit-2026-06-01-3625f0d.md + docs/audits/audit-2026-06-03-3625f0d.md
audit-lineage: 3625f0d 两份全量审计(06-01 A+B 合并 / 06-03 23维度独立) → 13 个修复提交(3625f0d..cdb3b03) → 本报告(修复提交严格复审)
status: completed
scope: 复审 3625f0d 之后 13 个修复提交的「修复有效性 + 回归 + 同根遗漏」，非重新全量审计
method: 12 维度 Workflow 独立复审(Phase1 不预筛保广度，许多 review agent 自建真实 harness 做 revert 测试) → 聚焦残留缝对抗验证(默认立场=误报/高估，真实 pre-tool-use.js 子进程端到端) → 2 个覆盖度 critic → 主 session 一手复跑全部 P0/P1 关键点 + 与两份 baseline 三方交叉
scale: 第一轮 50 agent / 1.23M tokens（Phase2 验证因额度中断，仅 review 层 11/12 维度产出）；第二轮 9 agent / 0.82M tokens 补对抗验证 + critic；主 session 一手 harness 4 组（PU-001/BG-01/残留/critic-new）+ 代码级核实
verification: test-pace-utils 199/199、test-hooks-e2e 274/274、test-agent-tests-helpers 9/9、plugin validate passed（基线绿灯，但 9a04 e2e 经实证 vacuous）
verdict: 修复主体在位且可安全发布；但守卫识别层重构仍是 blocklist、结构性不完整——baseline 两轮点名的 ln/xargs/反引号 仍开口，且 PSG-03/BG-05 两处修复以旧洞换新洞。最高优先后续=按 baseline 系统性建议 #1 收敛白名单/目标解析，而非再补 blocklist。
---

# audit-2026-06-05-3625f0d-to-cdb3b03: 修复提交严格审计

## 审查摘要

- **审查对象**: `git range 3625f0d..cdb3b03`（HEAD），即 3625f0d 两份全量审计之后的 **13 个修复提交**。本轮不是重新全量审计，而是对「修复有效性 / 回归 / 同根遗漏」三个攻击面的严格复审。
- **审计血缘**: `3625f0d` 被两份报告审出 1 P0(A01) + PU-001(P0) + BG-01族(P0) + 一批 P1/P2/P3 + TEAMMATE 系统盲区。13 个提交集中修复了这些 finding。本报告核实「修复是否真解决原 finding，还是只挡住报告里那个具体 PoC」。
- **核心结论**: **修复主体扎实**——2 个 P0（PU-001 路径伪造批准、BG-01 守卫绕过主形态）+ 约 20 个 P1/P2 经 revert 测试 + 真实子进程端到端确认 fixed，无数据丢失类回归，可安全发布。**但守卫识别层重构（0426773）虽根治了 bash↔powershell「算法不对称」（抽 `command-recognition.js` 共享），却没有升级判定模型——仍是 blocklist 锚点枚举**，导致：
  1. **baseline 两轮已点名的绕过向量仍开口**：`ln -sf` 符号链接替换（06-03:115 白纸黑字）、裸 `xargs rm`（05-15 + 06-03 两轮点名）、反引号命令替换（05-15 点名）——「点名未修 / 两轮点名两轮未修」。
  2. **两处修复以旧洞换新洞**：PSG-03 反引号剥离反噬 PowerShell `` `n ``/`` `r `` 语句分隔符（新 runtime-control 绕过）；BG-05 open 收窄引入关键字参数写绕过（重开 bbcc047 前已封堵的洞）。
- **证据基线**: 当前代码 + 真实 hook 子进程端到端为主，测试/revert 为佐证；文档仅作设计意图线索。全部 P0/P1 关键点经主 session 一手 harness 复跑。

### 运行验证（基线绿灯）

| 套件 | 结果 | 备注 |
|------|------|------|
| `node tests/test-pace-utils.js` | ✅ 199/199 | 较 3625f0d(160) +39，修复补测 |
| `node tests/test-hooks-e2e.js` | ✅ 274/274 | 较 3625f0d(257) +17；但 **9a04 经实证 vacuous**（见 VT-01）|
| `node tests/test-agent-tests-helpers.js` | ✅ 9/9 | 较 3625f0d(7) +2 |
| `claude plugin validate ./plugin` | ✅ passed | — |
| 版本三处一致 | ✅ v6.1.2 | plugin.json / marketplace.json / constants.js |

### 计数

| 项 | 数据 |
|----|------|
| 复审维度 | 12（守卫绕过/路径伪造/解析生命周期/v5迁移/compact/并发fail-open/TEAMMATE/测试有效性/ARCHIVE/发布文档/回归扫描）|
| baseline finding 修复确认 fixed | ~25（2 P0 + 多数 P1/P2，revert 测试 + 子进程端到端）|
| 确认残留（fix 不完整/同根遗漏）| **P1×1 簇（守卫识别层）+ P2×5 + P3×6** |
| 修复引入的新缺陷（regression）| **2**（PSG-03 `` `n `` 反噬 / BG-05 open 关键字参；均归 P1 簇/P2）|
| 对抗验证推翻/降级 | TM②③（teammate legacy-v5/strong-signal 软化）= BY-DESIGN（REFERENCE 5.1 已文档化）|
| 主 session 一手实证 | PU-001 全形态 / BG-01 全绕过形态 / ln / 反引号 / xargs / if-then / ed / open关键字 / PowerShell `` `n `` |

---

## 一、修复有效性总览（确认 fixed）

> 全部经 review 维度一手验证 + 多数用 revert 测试（改回旧代码看测试 FAIL）证明非 vacuous，关键项经主 session 复跑。

| baseline finding | 提交 | 状态 | 一手证据 |
|---|---|---|---|
| **PU-001**(P0) 路径不规整伪造批准 | 0426773 | ✅ fixed | `resolveToolFilePath:60` `path.posix.normalize` 折叠 `.`/`..`（刻意避 `path.resolve` 保 `C:/` 盘符）；`./`//`//`//`sub/../`//反斜杠`\.\` 全折叠回受保护路径，真实子进程 Edit `./changes/x.md` 注入 APPROVED 现 DENY |
| **BG-01族**(P0) 守卫段首锚点绕过 | 0426773 | ✅ 主形态 fixed | `MUTATING_ANCHOR` extraChars=`\n;&\|(){}`+wrapper 剥离+`do`；for..do/`{}`/`$()`/env/sudo/`&&`/换行/裸 rm 全 DENY（runtime-control + artifact 双门）|
| A02/PSG-04 over-block | 0426773 | ✅ fixed | script-engine 门保住：`cat task.md\|grep writeFileSync`、PS `Get-Content\|Select-String`、`open(,'r')` 只读全 ALLOW |
| A08 PowerShell 别名 | 0426773 | ✅ fixed | `ri/rni/cpi/mi/si/spi/rd/clc` 删锁全 DENY；PS git checkout/restore/reset 还原拦截与 bash 对称 |
| BG-02 in-place 编辑器 | 0426773 | ✅ 主形态 fixed | sed --in-place/perl -i -pe/sponge/gawk -i inplace/ex/vim -es DENY（**漏 ed**，见残留）|
| BG-03 单引号 redirect | 0426773 | ✅ fixed | `scanRedirectTargets` escapeChar 单引号内不生效，`echo 'C:\' > task.md` DENY |
| BG-04 script-engine 路径前缀 | 0426773 | ✅ fixed | `/usr/bin/node -e`、`env node` 识别；`nodejs`/`mynode` word-boundary 不误判 |
| BG-05/06 open 写模式/ruby-php | bbcc047 | ⚠️ partial | 只读 open 不 over-block ✅，但引入关键字参绕过（HCR-01）|
| A04 非 artifact 信号 Edit 对称 | 826893e | ✅ fixed | manual 信号无 changes/ 时 Edit task.md 现 DENY_DIRECT_ARTIFACT_EDIT（**但 e2e 测试 vacuous**，VT-01）|
| A05 operation 尾随文字 | 826893e | ✅ fixed | `firstToken` 取首 token，`operation: close-chg 顺便归档`+缺字段 → DENY |
| A06 v5 ignored 死锁 | 826893e | ✅ fixed | `v5MigrationPromptMessage:404` `!needsPrompt` 抑制；ignored 后 Stop EMPTY 放行 |
| A07 大写 [X] 污染 | 826893e | ✅ fixed | `parseChangeIndex:158` `.toLowerCase()` 解析层归一，全 6 hook 共享 |
| PSP-02 关错 owner | 826893e | ✅ fixed | inferCloseTarget operation/target 同源，不同源降级 SKIP |
| A03 迁移回滚盲区 | c84227e | ✅ fixed | 写显式 migrated state + 回滚指引补 `rm -rf changes/` |
| MIGV5-01 --force 销毁备份 | c84227e | ✅ fixed | 实跑迁移→删backup→--force，rejected，ARCHIVE 计数恒=1 |
| MIGV5-02 迁移非原子 | c84227e | ✅ 主路径 fixed | 两阶段+回滚，非 force 路径回滚逐字节保真（**force+hasBackup 路径有回归**，MIGV5-02-REG）|
| MIGV5-03 transformV5Body | c84227e | ✅ 主形态 fixed | frontmatter 起止 `^---$` + looksLikeYaml |
| HOTFIX compact 截断方向 | da91f1e | ⚠️ 逻辑正确但死路径 | tie-breaker 翻转正确，但所修的 `## YYYY-MM-DD` 段落格式当前 artifact-writer 不产出（HCR-02）|
| PUC-02/ROB-01 stdin null 崩溃 | 574c80e | ✅ fixed | `session.js:26` 归一 + try/catch，`echo null\|node pre-tool-use.js` exit 0 |
| PUC-01 VAULT_PATH 降级 | 574c80e | ✅ fixed | `stop.js:33` vault-env-missing → exit 2 不静默降级 |
| STOP-02 counter 死循环 | 574c80e | ✅ fixed | setBlockCount 返回成败，不可写→exit 0 + COUNTER_UNWRITABLE |
| PL-01 synced-plans CRLF | 574c80e | ✅ fixed | `plans.js:59` normalizeLineEndings + trim |
| H-01 set-artifact-root 吞参 | 574c80e | ✅ fixed | argv peek，`--choice --cwd /x` → DENY_MISSING_VALUE |
| FC-01 correction sync fail-open | 574c80e | ✅ fixed | `post-tool-use.js:206` `&& (newString \|\| content)` |
| ROB-02 空锁抢占 | 574c80e | ⚠️ mitigated | INFLIGHT_GRACE_MS 宽限闭合常态竞态，但 temp+rename 原子上线未实现（mtime 不可信窗口残留）|
| STOP-03 无 sid foreign-fresh | bbcc047 | ✅ fixed | 返回 unknown 不算 foreign，不跳过可能属己的 running CHG |
| PU-002 hasChangesDir 同名文件 | bbcc047 | ✅ fixed | `isDirectory()`，两份副本同步 |
| TEAMMATE 写代码门升 hardDeny | 03c1b6b | ✅ 主体 fixed | C/E/无活跃/索引三门 `hardInTeammate:true`，teammate 下硬拦（**shell 删 artifact 仍软，TM-①**）|
| TS-01/W2 日志 delta 守卫 | e0a168e | ✅ fixed | 全 4 处 delta 用 logDelta()/inline 守卫，无裸 slice |
| ATF-02 raw 空 fail-open | e0a168e | ✅ 主体 fixed | failure_pattern/raw_must_contain 无条件硬判（**默认 title-strict 仍 fail-open**，P3）|
| ARCHIVE 缺失两层防御 | 9eed534 | ✅ 主体 fixed | 层1 checkArchiveFormat 缺失警告 + 层2 截断兜底（**CJK 内容 footer 不可达**，ARCH-01）|
| W3 close-chg 全[-] | 273173f | ✅ fixed | close-chg.md:63 format-violation 守卫 |
| DOC-A1~A6/CFG-01/版本一致/LOCKS-001 | 273173f/a378d84/bbcc047 | ✅ fixed | 逐条 grep 核实（仅 REFERENCE「最后更新」日期 stale，P3）|

---

## 二、确认残留与新缺陷

### P1：守卫识别层 blocklist 结构性不完整（同根残留簇）

#### [RES-GUARD] 守卫绕过类未尽残留——baseline 点名未修 + 修复引入新洞（✅ confirmed P1，主 session + 对抗验证 + critic 三方端到端实证）

- **根因**: `0426773` 抽取 `command-recognition.js` 共享识别层，**根治了 bash↔powershell「算法不对称」**（两侧共用 `segmentAnchorPrefix`/`scanRedirectTargets`/`commandRunsScriptEngine`），并把锚点字符集扩到 `\n;&|(){}`+wrapper 剥离+`do`，堵住了**最自然的删锁/改 artifact 写法**（裸 rm、rm -rf、for..do、`{}`、`$()`、env/sudo、`&&`、换行）。**但判定模型仍是 blocklist 锚点枚举，非 baseline 反复建议的白名单/目标解析**，故同根漏判持续：

| 残留向量 | 门 | 实测 | baseline 是否点名 | 性质 |
|---|---|---|---|---|
| `ln -sf /dev/null .pace/artifact-writer.lock` | runtime-control hardDeny | **ALLOW** ❌ | ✅ 06-03:115 白纸黑字 | 点名未修（MUTATING_VERB_SOURCE 无 ln）|
| `ln -f x task.md` | artifact-write | **ALLOW** ❌ | ✅ 同上 | 点名未修 |
| `echo x \| xargs rm`、`ls changes/*.md \| xargs rm` | 双门 | **ALLOW** ❌ | ✅ 05-15 W-3 + 06-03:115 两轮 | 两轮点名两轮未修（只修 `xargs bash -c`）|
| `` echo `rm .pace/artifact-writer.lock` ``、`` `rm task.md` `` | 双门 | **ALLOW** ❌ | ✅ 05-15 点名反引号 | 点名未修（反引号未当段首边界，`$()` 反而堵了——不对称）|
| `if true; then rm .pace/locks/x; fi` | 双门 | **ALLOW** ❌ | 隐含同根 | then 非 wrapper（`do` 加了，`then`/`else` 没加）|
| `ed .pace/artifact-writer.lock`、`ed task.md` | 双门 | **ALLOW** ❌ | ✅ 06-03 BG-02 点名 ed | 点名未修（INPLACE_EDITOR 补了 ex/vim 漏 ed）|
| `` Write-Host hi`nRemove-Item .pace/artifact-writer.lock `` | PS runtime-control | **ALLOW** ❌ | 新引入 | **PSG-03 反噬**：剥反引号转义把 `` `n `` 黏成字面 n，失锚 |
| `$x = $(Remove-Item .pace/artifact-writer.lock)` | PS runtime-control | **ALLOW** ❌ | 新引入/同根 | looksMutating=true 但 `$()` 内路径 token 取不到 → ref=0 不拦 |
| `python -c "open('task.md', mode='w')"` | artifact-write | **ALLOW** ❌ | 新引入 | **BG-05 反噬**（见 HCR-01）|

- **主 session 一手证据**: `node require bash-guard.js`/`powershell-guard.js` 直调真实 gate 函数 `bashCommandMutatesArtifactRuntimeControl`/`bashCommandLooksMutating`/`powershellCommandMutatesArtifactRuntimeControl`——上表全部复现；对照 fix 已堵形态全 DENY。对抗验证员另起**真实 pre-tool-use.js 子进程**端到端坐实（control `rm .pace/locks/x` → permissionDecision:deny；残留形态 → stdout 空=放行）。
- **影响**: 触碰的是 runtime-control hardDeny（删 `.pace/locks/*`/`artifact-writer.lock`/sequences/index-transactions → 破坏 ID 唯一性/索引事务，**非 git 可恢复**；teammate 下 hardDeny 本不降级，但锚点漏判使其整体不触发）+ artifact-write。**与原 BG-01(P0) 同等影响**。
- **定级 P1（非 P0）**: 最自然的删锁写法已全堵，残留形态需 AI 主动选用非常规写法，可达性中。**例外提醒**：PowerShell `` `n `` 是单行多语句**最地道**写法，对 Windows 用户可达性偏高，单论该条接近 P1-高。
- **修复建议（按 baseline 系统性建议根治）**: 不要再补 blocklist。① 把 mutating 判定从「扁平 token 存在性 OR + 段首锚点」收敛为 **redirect 路径式 source/target 解析**，或对触碰 `.pace` 运行态/artifact 的命令改 **白名单**（仅放行明确只读 + artifact-writer 路径）。② 若短期仍走 blocklist，至少补：`ln` 入 MUTATING_VERB_SOURCE；`xargs(\s+-\S+)*` 入 wrapper 剥离链；`then`/`else` 入循环/条件 wrapper；反引号内容递归当子命令；`ed` 入 INPLACE_EDITOR；PowerShell 归一化前先把 `` `n ``/`` `r ``/`` `t `` 转空白/分隔（不要剥成字面）；`$()` 内路径 token 剥包裹后再匹配；open 正则容忍 `\w+=` 关键字前缀。③ **务必 TDD 补 e2e**：反引号/xargs/if-then/ln/ed 删 .pace/locks 与改 task.md 必须 deny，bash↔powershell 对称补测。

### P2 残留

#### [HCR-01] BG-05 open 收窄引入关键字参数写绕过（✅ confirmed P2，主 session 完整决策三路实证）

- **位置**: `bash-guard.js:36`(MUTATING_PATTERNS open 正则) + `:126`(commandTextContainsWriteApi) + `:429`(scriptSourceWritesArtifactTarget)，均假定位置参数
- **实测（完整 artifact-write 决策 looks\|\|embeds\|\|redirect 三路）**: `open('task.md', mode='w')` / `open(file='task.md', mode='w')` / `open('task.md', encoding='utf8', mode='w')` → **三路全 false = DENY=false 绕过**；位置参 `open('task.md','w')` 与 `writeFileSync` 正确 DENY；pathlib `write_text`（含 `;`）被 embeds 兜住非绕过
- **性质**: BG-05 修 over-block 时把写模式正则收窄为「逗号后紧跟引号+模式」，关键字 `mode=` 插在中间使其失配——**重开了 bbcc047 前已封堵的洞**（pre-BG-05 裸 `open\s*\(` 会拦此形态）。附带新引入 bash↔powershell 不对称（`powershell-guard.js:118` 仍裸 `open\s*\(` 未收窄）
- **影响**: artifact-write（denyOrHint，主 session deny/teammate 软提示，git 可恢复）；可达性低（AI 改 artifact 走 Write/Edit，Python 惯用位置参）。封顶 P2
- **建议**: 两处 open 正则容忍 `mode=` 关键字前缀；BG-05 收窄移植到 powershell 侧消除新不对称；可并入守卫重构 CHG，不必紧急 HOTFIX

#### [TM-①] TEAMMATE 下 shell 破坏性删/覆 artifact 仍软放行（与 Edit/Write hardDeny 不对称）（✅ confirmed P2，对抗验证子进程实证）

- **位置**: `pre-tool-use.js:725`(bash)/`:751`(powershell)/`:778`(Monitor) 的 `denyOrHint(reason)` 未传 hardInTeammate
- **实测**: `CLAUDE_CODE_TEAM_NAME=alpha` 下 `rm <dir>/changes/chg.md`、`echo x > task.md` → non-teammate=deny / teammate=HINT(放行)；而**同一文件**直接 `Edit task.md` → 两态都 hardDeny。**同一破坏性意图、同一文件，因工具不同处置档相反**
- **裁决修正**: finding 原列三类，对抗验证逐项判——② legacy-v5 写代码门、③ strong-signal/code-count 首写门 = **BY-DESIGN**（REFERENCE 5.1 明列为 tier-1 流程引导类有意软化，硬拦会死锁 teammate）。仅 **① shell 破坏性写 = 真实完整性缺口**（破坏性写非流程引导，不存在死锁风险）
- **建议**: `:725/:751/:778` 改 `denyOrHint(reason, { hardInTeammate: true })`，与「直接 Write/Edit artifact 走 hardDeny」对齐

#### [MIGV5-02-REG] 两阶段原子化重构在 force+hasBackup 路径引入回滚保真度回归（✅ confirmed P2，实跑 sha 逐字节比对）

- **位置**: `batch-archive-v5.js:137`(force&&hasBackup 把 sourcePath 设 backupPath) + `:193`(push `before: p.original`) + `:172`(回滚写回)
- **实测**: 非 force 主路径回滚**逐字节保真**（4 文件 sha 全等）；force+hasBackup 路径回滚把文件写成**原始 v5** 而非**调用前真实状态**（sha 比对：==backup true / ==currentMain false）。预存 changes/ 与 corrections.md 完整保留
- **影响**: 仅 force+hasBackup 窄路径，非默认；数据完整性偏差但非丢失。P2
- **建议**: `:193` push 前 `const cur = fs.readFileSync(p.filePath)` 快照真实当前字节，回滚写回 cur；补 force 路径回滚保真测试

#### [ARCH-01] ARCHIVE 层2 截断对 CJK findings 被全局 46KB 字节守卫抢先，缺失警告 footer 不可达（✅ confirmed P2，真实 session-start 子进程实证）

- **位置**: `session-start.js:18-19`(SESSION_OUTPUT_BUDGET_BYTES=46000 全局**字节**守卫，:66 先于注入安装) vs `:424-427`(层2 保留 30000 **字符** + footer) vs `constants.js:13`(ARCHIVE_MISSING_INJECT_LIMIT=30000 字符)
- **单位错配**: 层2 保留 30000 字符，CJK ~3 bytes/字符 ≈ 90000 bytes；全局守卫在 46000 bytes（CJK 约第 15000 字符处）已截断字节流并丢弃后续写入 → footer（约第 30000 字符处）**永不可达**。截断仍发生（context 不被灌爆），但「ARCHIVE 缺失」**告警信号对中文内容丢失**
- **影响**: 层2 的「报警」目的对 CJK 失效（防灌爆目的仍达成）。P2
- **建议**: 层2 改按 UTF-8 字节截断（`sliceUtf8ToBytes`，LIMIT 设 20000~25000 bytes < 46000），或把缺失警告**前置**输出（全局守卫触发前）

#### [VT-01] A04 e2e 用例 9a04 vacuous——A04 fix 全禁用仍 274/274（✅ confirmed P2，复制 repo revert 实证）

- **实测**: 把 `pre-tool-use.js:1159` A04 块条件改 `if (false && …)`，重跑全套 e2e = **仍 274/274 且显式 PASS:9a04**。逐守卫日志取证：9a04 fixture 的 legacy-v5 task.md（`## 活跃任务` 无 changes/）触发上游 v5 迁移门(`:1445` DENY_LEGACY_ACTIVE)，其文案同含 `"deny"` 与 `artifact-writer`，使 9a04 两条 includes 断言在 A04 在/不在两态都通过
- **澄清**: A04 **代码 fix 真实有效**（review 真实子进程 manual 信号 Edit task.md=DENY 已证）；**仅测试无判别力**，无法守护 A04 回归
- **建议**: 9a04 fixture 改 code-count/manual 信号（无 v5 签名）+ 中和 vault routing 使 artDir===cwd，Edit 无 v5 标记的 managed artifact，使 A04 移除时变 FAIL

### P3 残留（建议 record-finding 归档，不阻塞）

- **HCR-02**(P3): `da91f1e` tie-breaker 修的 `## YYYY-MM-DD` walkthrough 详情段落格式当前 artifact-writer 契约**不产出**（close-chg 写表格行到 `## 最近工作`，非日期标题段落）。修复逻辑正确但针对**当前不可达路径**；代码无害但误导。建议删该块+测试 2e2，或注释标注 legacy-tolerance 定位
- **ATF-02 残留**(P3): 默认 report_title_strict（exp 未显式设）对空 raw 仍走 else 跳过 = fail-open；仅显式 title-strict 被硬化。属有意收窄但无测试钉死
- **VT-02**(P3): e2e 9pu1 弱判别（仅断言 includes('deny')，PU-001 layer1 单独 revert 仍 PASS，靠通用 DENY_DIRECT_ARTIFACT_EDIT 兜底）；layer1 由 test-pace-utils 单测覆盖，非完全无护栏
- **MIGV5-03-INC**(P3): 开头 `---` + 含冒号散文行仍误判 frontmatter（仅渲染瑕疵无数据丢失）
- **A03-INC**(P3): 用户漏删 migrated state 时仍被抑制（属用户未照指引，非代码缺陷）
- **sweepStaleRuntimeOwners 边界**(P3，已 REFUTE 留痕): 纯 mtime 删 active owner，但 heartbeat 只刷新同 session owner，被删恒为真孤儿；唯一残余=同 artifact-root 第二 session 误扫首 session 长 idle owner，属已 accepted 的 LOCKS-001/RSL 跨 runtime 边界

---

## 三、系统性结论

1. **守卫识别层重构「治了一半」**: `0426773` 正确根治了 baseline 系统性风险「bash↔powershell 手工移植**算法不对称**」（抽 `command-recognition.js` 单一识别层，两侧传数据），并堵住最自然的绕过形态。**但 baseline 真正的 #1 建议是「收敛白名单 / redirect 式 source/target 解析」——这一步没做**。仍是 blocklist 锚点枚举，故 `ln`/`xargs`/反引号/`then`/`ed`/open关键字 等未枚举形态持续漏判，**baseline 两轮点名的向量仍开口**。这是修复审计最精微的判断：fix 解决了它声称的那层（不对称），未解决底层模型（blocklist 不完整）。

2. **两处修复以旧洞换新洞，且重新制造不对称**: PSG-03 为修「反引号转义路径绕过」用无差别 `` `([\s\S]) `` 剥离，反噬 PowerShell `` `n ``/`` `r `` 语句分隔符（新 runtime-control 绕过）；BG-05 为修「只读 open over-block」收窄写模式正则，引入关键字参数写绕过（重开旧洞）+ 只改 bash 侧制造新 bash↔powershell 不对称。**印证 baseline 论断：blocklist 手工移植每修一处易在别处漏/反噬。**

3. **测试只锚定点名 PoC**: 守卫残留（ln/xargs/反引号/then/ed/`` `n ``/open关键字）**全部 0 测试覆盖**；新测试只覆盖 commit 点名的 for..do/别名等。加上 9a04 vacuous——「全绿」对这些维度无护栏。

4. **修复主体安全**: 除守卫识别层外，解析/生命周期/迁移/并发/fail-open/ARCHIVE/文档类修复扎实，多数有 revert 测试护栏，无数据丢失类回归。da91f1e/273173f 的 session-start/pre-compact 大行数改动经 `git show -w` 证实绝大部分是 CRLF→LF 归一，真逻辑改动极小，无回归。

---

## 四、验证矩阵

| 发现 | 验证方法 | 终判 |
|---|---|---|
| PU-001 修复 | 主 session 真实子进程 + 全形态 harness（./ // sub/../ 反斜杠）| ✅ fixed |
| BG-01 主形态 | 主 session gate 函数直跑 + 对抗验证子进程端到端 | ✅ fixed |
| RES-GUARD 残留簇 | 主 session harness（ln/反引号/xargs/then/ed/`` `n ``/open）+ 对抗验证真实 pre-tool-use.js 子进程 + critic 端到端 | **确认 P1 残留** |
| HCR-01 open 关键字 | 主 session 完整决策三路（looks/embeds/redirect 全 false）| **确认 P2 回归** |
| PSG-03 `` `n `` 反噬 | 主 session powershellCommandMutatesArtifactRuntimeControl（`` `n `` ALLOW vs `;`/真换行 DENY）| **确认 P1 回归** |
| TM-① shell 删 artifact 软放行 | 对抗验证真实子进程 teammate=HINT vs Edit hardDeny | **确认 P2**（②③ BY-DESIGN）|
| MIGV5-02-REG | 实跑 archiveV5 + 故障注入 + sha 逐字节 | **确认 P2**（仅 force 路径）|
| ARCH-01 CJK footer | 真实 session-start 子进程 + 字节/字符单位追踪 | **确认 P2** |
| VT-01 9a04 vacuous | 复制 repo + A04 块 revert 重跑 274/274 | **确认 P2 测试缺口** |
| A05/A06/A07/PSP-02/A04 代码 | 主 session 代码级 + review 真实子进程 + revert 测试 | ✅ fixed |
| 并发/fail-open 7 处 | review 真实子进程 + bundled e2e | ✅ fixed（ROB-02 mitigated）|
| 测试套件绿灯 | 主 session 实跑 | 199/274/9（9a04 vacuous）|

---

## 五、建议后续动作

- **CHG-守卫识别层根治（最高优先，承接 baseline 两轮 #1 建议）**: 把 mutating/write-API/runtime-control 判定从 blocklist 锚点收敛为 **白名单 / redirect 式 source-target 路径解析**。短期补丁（若不立即重构）：`ln`/`then`/`else`/`xargs`/反引号递归/`ed`/`$()`token剥包裹/open关键字参/PowerShell `` `n `` 归一化修正——并 **bash↔powershell 对称补测 + e2e 钉死全部残留向量**。
- **HOTFIX-TM①**: `pre-tool-use.js:725/751/778` 升 `hardInTeammate:true`（shell 破坏性写 artifact 在 teammate 下不降级）。
- **修补 P2**: HCR-01（open 关键字正则 + powershell 对称）、MIGV5-02-REG（force 路径回滚快照真实当前字节）、ARCH-01（层2 按字节截断或前置 footer）、VT-01（9a04 fixture 去 v5 信号使其有判别力）。
- **P3 归档**: 派 `artifact-writer record-finding` 把 HCR-02/ATF-02残留/VT-02/MIGV5-03-INC/sweep 边界归档到 `changes/findings/`。
- **延续「先审不修」**: 本报告只产出审计，修复另开 CHG/HOTFIX。

---

## 六、审计盲区（未覆盖/未独立验证）

1. **第一轮 Workflow Phase2 对抗验证因额度中断**：12 维度 review 仅 11 维度产出（PU-001 维度部分），37 条原始 finding 的对抗验证大面积失败，由第二轮聚焦补做 + 主 session 一手 + 两份 baseline 三方交叉补偿；但 review 层原始 37 条的逐条对抗未全覆盖，以主 session 实证的 P0/P1 + 第二轮 9 条裁决为权威。
2. **PowerShell 侧残留以静态 + helper 实测为主**：`` `n `` / `$()` 经 require 模块实测，但未全部起真实 pre-tool-use.js 子进程端到端（bash 侧已端到端）。
3. **真实多进程并发**（ROB-02 mtime 窗口、LOCKS-001 跨 runtime、sweep 第二 session 误扫）未起真实并发竞态，属单进程 fixture/静态推断。
4. **da91f1e/9eed534 极窄交互**（WALK-02 层2 警告被 T-386 截断）单点实测，未穷举。
5. **install.js/verify.js/test-install.js** git-untracked 本地工具，按 `paceflow/CLAUDE.md` 不在 marketplace scope，正确排除。

---

## 证据来源

- **源码**: `plugin/hooks/pre-tool-use/{command-recognition,bash-guard,powershell-guard,agent-lifecycle-guard,marker-guard}.js`、`pre-tool-use.js`、`pace-utils/{path-utils,change-analysis,locks,plans,session,constants}.js`、`pace-utils.js`、`post-tool-use.js`、`stop.js`、`subagent-stop.js`、`session-start.js`、`migrate/batch-archive-v5.js`、`tests/agent-tests/helpers/verify-output.js`
- **主 session 一手 harness**: `Temp/pf-fixaudit/{harness,residuals,hcr01,critic-new}.js`——直调真实 gate 函数；PU-001 全形态、BG-01 全绕过、ln、反引号、xargs、if-then、ed、open关键字、PowerShell `` `n ``
- **对抗验证子进程**: 真实 `pre-tool-use.js`/`session-start.js`/`stop.js` 子进程 + 隔离 v6 fixture（control DENY vs 残留 ALLOW 端到端）；archiveV5 实跑 + sha 比对
- **提交**: `git range 3625f0d..cdb3b03`（0426773/826893e/c84227e/da91f1e/574c80e/03c1b6b/e0a168e/273173f/a378d84/bbcc047/9eed534/eab5e1f/cdb3b03），`git show -w` 区分逻辑/行尾归一
- **测试**: test-pace-utils 199/199、test-hooks-e2e 274/274（9a04 vacuous）、test-agent-tests-helpers 9/9、plugin validate passed
- **baseline 交叉**: `docs/audits/audit-2026-06-01-3625f0d.md`、`audit-2026-06-03-3625f0d.md`（系统性风险 #1、BG-01 点名向量 :115）

---

## 后续测试建议（只列能复现确认残留的最小测试）

1. **RES-GUARD**: 非 owner session `ln -sf /dev/null .pace/artifact-writer.lock`、`echo x \| xargs rm`（喂 .pace/locks 或 changes/）、`if true; then rm .pace/locks/x; fi`、`` echo `rm task.md` ``、`ed task.md` 必须 deny；PowerShell `` Write-Host x`nRemove-Item .pace/artifact-writer.lock ``、`$x=$(Remove-Item <lock>)` 必须 deny。bash↔powershell 对称。
2. **HCR-01**: `python -c "open('task.md', mode='w')"` / `open(file='task.md', mode='w')` 写 artifact / 写 .pace 运行态必须 deny；只读 `open(f)` 不 over-block。
3. **TM-①**: teammate 模式 `rm <changes>/x.md`、`echo x > task.md` 必须 deny（当前 HINT 放行）。
4. **MIGV5-02-REG**: force+hasBackup 路径注入阶段二写失败，断言所有主文件逐字节还原**调用前**状态（当前还原为原始 v5）。
5. **ARCH-01**: 超大 **CJK** findings.md（无 ARCHIVE 标记）起 session-start，断言注入输出含「ARCHIVE 缺失」警告 footer（当前被 46KB 字节守卫截断丢失）。
6. **VT-01**: 重构 9a04 使 A04 Edit 块被移除时 e2e FAIL（当前 vacuous）。
