# v6 CHG 详情模板别名

> 保留此文件名是为了兼容旧 skill 引用；v6 不再把变更详情写进 `implementation_plan.md`。

请使用同目录的 `change-detail.md` 作为唯一 CHG 详情模板。真正的详情文件由 `artifact-writer create-chg` 创建在 `changes/<id>.md`，并由 `task.md` 与 `implementation_plan.md` 通过 wikilink 索引。
